syntax = "proto3";

package ciao.common;

// A combined object for all parts of a name for a person
message Name {
  // First name
  string first = 1;
  // Middle name (if available)
  string middle = 2;
  // Last name
  string last = 3;
}
