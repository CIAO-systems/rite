targets:
  $default:
    sources:
      - $package$
      - lib/$lib$
      - proto/**  # Directory containing your .proto files
    builders:
      protoc_builder:
        options:
          root_dir: "proto/"
          out_dir: "lib/src/generated"
          grpc: true  # Set to true if you're using gRPC
