<mapper>
   <field>
     <name source="id" target="account" />
     <type source="string" target="string" />
     <values>{% for record in records %}
         <!-- Time type: {{ record.name }} -->
        <value source="{{ record.id }}" target="{{ loop.index }}" />{% endfor %}
     </values>
   </field>
</mapper>